name: Cập nhật tin tức (Bến Hàng Hóa)

# Tự chạy 2 LẦN/GIỜ (phút :25 và :55) + cho phép bấm chạy tay trong tab Actions.
# ⚠️ KHÔNG đặt phút :00 — đầu giờ GitHub kẹt tải nặng nhất, free tier hay BỎ QUA
#    lịch chạy. Dời sang phút lẻ (:25/:55) để chạy đáng tin cậy hơn nhiều.
# Web đọc tin trực tiếp từ GitHub raw nên cập nhật KHÔNG tốn build Cloudflare
# (cần loại public/data/news.json khỏi "Build watch paths" của Cloudflare Pages).
on:
  schedule:
    - cron: "25,55 * * * *"   # phút :25 và :55 mỗi giờ (UTC) — tránh đầu giờ kẹt
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-news:
    runs-on: ubuntu-latest
    steps:
      - name: Lấy mã nguồn
        uses: actions/checkout@v4

      - name: Cài Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      # Hai khoá AI là LỚP DỰ PHÒNG CUỐI cho khâu dịch. Bình thường bot dịch bằng
      # máy dịch miễn phí; chỉ khi cả hai máy dịch bị chặn mới nhờ đến AI.
      # Thiếu khoá thì bot vẫn chạy, chỉ mất lớp đỡ cuối cùng.
      - name: Chạy bot lấy + dịch tin
        run: node scripts/fetch-news.mjs
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GROQ_API_KEY: ${{ secrets.GROQ_API_KEY }}

      - name: Commit news.json nếu có thay đổi
        run: |
          git config user.name  "benhanghoa-bot"
          git config user.email "bot@benhanghoa.local"
          git add public/data/news.json
          if git diff --staged --quiet; then
            echo "Không có tin mới."
            exit 0
          fi
          git commit -m "chore: cập nhật tin tức tự động"
          # Bot này chạy dày nhất (2 lượt/giờ theo lịch + Worker gọi mỗi đầu giờ)
          # nên dễ có hai lượt cùng đẩy một lúc, lượt sau bị kho từ chối. Thử lại
          # thay vì báo đỏ oan — xem ghi chú dài hơn trong fetch-prices.yml.
          for lan in 1 2 3 4 5; do
            if git push; then
              echo "Đã lưu phiên bản và đẩy lên (lần thử $lan)."
              exit 0
            fi
            echo "Đẩy hỏng ở lần thử $lan — kéo bản mới nhất về rồi thử lại."
            git pull --rebase --autostash origin "${{ github.ref_name }}" || true
            sleep $((lan * 5))
          done
          echo "::error::Đẩy news.json lên kho thất bại sau 5 lần thử — không phải lỗi khâu dịch, mà lỗi khâu lưu."
          exit 1
