# OS
.DS_Store
Thumbs.db
desktop.ini

# Editor
.vscode/
.idea/
*.swp

# Node (nếu sau này thêm)
node_modules/
npm-debug.log*

# Astro build artifacts (Cloudflare tự build, không cần commit)
dist/
.astro/

# Wrangler (Cloudflare Worker) cache
.wrangler/

# Logs / tạm
*.log
.cache/

# Công cụ NỘI BỘ — KHÔNG đăng công khai (chứa dữ liệu đọc từ TradingView Premium)
bang-gia-premium.html
data/gia.json

# Thông tin riêng — KHÔNG đưa lên GitHub (kho mã này đang để CÔNG KHAI)
# Chứa link quản trị, địa chỉ thư điện tử, tên tài khoản.
# Đã lên GitHub một lần là còn trong lịch sử, xoá cũng không sạch.
HUONG-DAN-VAN-HANH-WEB.md
SO-TAY-VAN-HANH-BEN-HANG-HOA.docx
CLAUDE.local.md
# Mọi bản sao lưu đặt theo quy ước "<tên>.bak-NĂMTHÁNGNGÀY".
# Chặn cả mẫu này vì bản sao lưu của một tập tin riêng tư thì cũng riêng tư —
# ngày 06/08/2026 đã suýt để CLAUDE.md.bak-20260806 lọt lên kho công khai.
*.bak-*

# Quy ước làm việc + công cụ sao lưu nội bộ — giữ riêng, sao lưu ở kho GitHub RIÊNG TƯ
# (xem F:\ben-hang-hoa-noi-bo). Chạy "npm run sao-luu" để cập nhật bản sao.
CLAUDE.md
.claude/
scripts/sao-luu-noi-bo.mjs
